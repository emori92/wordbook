
'user strict';

import { changeTabDisplay } from '../script/functions.js';


// function parameter
const dashboardParameter = {
  'sessionName': 'dashboard',
  'idList': ['wordbook', 'follow', 'liked']
}

changeTabDisplay(dashboardParameter);
